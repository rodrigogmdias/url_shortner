name: app
description: "A url shortner"

resolution: workspace
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  cupertino_icons: ^1.0.8

  flutter:
    sdk: flutter

  home:
    path: ../packages/features/home

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0