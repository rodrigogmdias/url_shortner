name: network
description: "A new Flutter package project."
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  get_it: ^8.2.0
  injectable: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  injectable_generator: ^2.8.1
  build_runner: ^2.8.0

  flutter_lints: ^5.0.0