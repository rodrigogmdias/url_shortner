name: home
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  router:
    path: ../../core/router
  design:
    path: ../../core/design

  get_it: 
  injectable: 
  bloc: 

dev_dependencies:
  flutter_test:
    sdk: flutter

  injectable_generator: 
  build_runner: 

  flutter_lints: 