name: history
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  storage:
    path: ../../core/storage
  design:
    path: ../../core/design

  get_it: 
  injectable: 
  meta:
  bloc:
  flutter_bloc: 

dev_dependencies:
  flutter_test:
    sdk: flutter

  injectable_generator: 
  build_runner: 

  flutter_lints: 